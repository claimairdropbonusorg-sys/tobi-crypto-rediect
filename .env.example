# Example environment variables for Tobi Crypto Redirect
# Copy this file to `.env.local` and customize values as needed. `.env.local` is gitignored.

# Public API base URL (CoinGecko by default)
NEXT_PUBLIC_API_URL=https://api.coingecko.com/api/v3
# Optional CoinGecko Pro API key (if you have one) to increase rate limits
NEXT_PUBLIC_COINGECKO_API_KEY=
# Default currency for price display
NEXT_PUBLIC_DEFAULT_VS_CURRENCY=usd

# Telegram bot config (server-side)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Admin secret for reading logs and admin pages
ADMIN_SECRET=
NOTIFY_API_KEY=
# (Optional) Public notify key to send from the client if you want client-originating events to be accepted. WARNING: setting a public key exposes it in client code.
NEXT_PUBLIC_NOTIFY_API_KEY=
